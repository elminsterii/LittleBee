<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayoutDrawer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorMainPageBackground"
    tools:context="elminsterii.littlebee.MainActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/my_toolbar"
        android:layout_width="368dp"
        android:layout_height="56dp"
        android:background="@color/colorTopBarBackground"
        android:elevation="4dp"
        android:theme="@style/CustomActionBar"
        app:popupTheme="@style/Theme.Custom" />

    <FrameLayout
        android:id="@+id/content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ListView
        android:id="@+id/listViewDrawer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="left"
        android:background="@color/colorDrawerBackground"
        android:choiceMode="singleChoice"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp" />

    <TextView
        android:layout_width="170dp"
        android:layout_height="62dp"
        android:gravity="center"
        android:text="@string/main_page_bg_string"
        android:textColor="@color/colorStandardGrayText"
        android:textSize="22sp" />

</android.support.v4.widget.DrawerLayout>
